export class Icons {
  name = '';
  identifier = '';
  isActive = false;
  url = '';
  constructor(name: string, identifier: string, url: string) {
    this.name = name;
    this.identifier = identifier;
    this.url = url;
  }
}
